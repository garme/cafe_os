# Ferramentas auxiliares

A compilação Kernel+Overlay agora é feita integralmente pela IDE/Compilador.

O antigo script `make_overlays_inc.py` não é mais necessário para o fluxo normal. Ele foi removido deste pacote para evitar confusão.
